#import "/template/template.typ": glossario 

= Requisiti 
Per usare l'applicazione è necessario solo un browser e una connessione verso un host che la esegua. Nel caso in cui si voglia eseguire e utilizzare l'applicazione sul medesimo host, si devono consultare i requisiti presenti nella specifica tecnica. 

== Requisiti hardware
#figure(
  table(
    columns: (1fr, 2fr),
    fill: (_, y) => if calc.even(y) { gray.lighten(50%) } else { white } ,
    align: center+horizon,

    [*Processore*],
    [],
    [*Memoria RAM*],
    [],
    [*Memoria *],
    [],
  ),
  caption: "Requisiti hardware."
)
== Requisiti di sistema
#figure(
  table(),
  caption: "Requisiti di sistema."
)